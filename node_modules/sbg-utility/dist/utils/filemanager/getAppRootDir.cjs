'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var fs = require('fs-extra');
var path = require('upath');
var url = require('url');
var pathUtility = require('./path-utility.cjs');

var _documentCurrentScript = typeof document !== 'undefined' ? document.currentScript : null;
const __filename$1 = url.fileURLToPath((typeof document === 'undefined' ? require('u' + 'rl').pathToFileURL(__filename).href : (_documentCurrentScript && _documentCurrentScript.tagName.toUpperCase() === 'SCRIPT' && _documentCurrentScript.src || new URL('utils/filemanager/getAppRootDir.cjs', document.baseURI).href)));
const __dirname$1 = path.dirname(__filename$1);
function getAppRootDir() {
  let currentDir = __dirname$1;
  while (!fs.existsSync(path.join(currentDir, 'package.json'))) {
    currentDir = path.join(currentDir, '..');
  }
  return path.toUnix(pathUtility.trueCasePathSync(currentDir));
}

exports.default = getAppRootDir;
exports.getAppRootDir = getAppRootDir;
