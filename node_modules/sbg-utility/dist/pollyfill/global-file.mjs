import { commonjsGlobal } from '../_virtual/_commonjsHelpers.mjs';
import { __exports as globalFile } from '../_virtual/global-file2.mjs';

var hasRequiredGlobalFile;
function requireGlobalFile() {
  if (hasRequiredGlobalFile) return globalFile;
  hasRequiredGlobalFile = 1;
  if (typeof File === 'undefined') {
    commonjsGlobal.File = class File extends Blob {
      constructor(parts, options) {
        super(parts, options);
        this.name = options && options.name ? options.name : '';
        this.lastModified = options && options.lastModified ? options.lastModified : Date.now();
      }
    };
  }
  return globalFile;
}

export { requireGlobalFile as __require };
